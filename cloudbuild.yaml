steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}', '--build-arg', 'NEXT_PUBLIC_FAUNADB_SECRET=${_NEXTPUBLIC_FAUNADB_SECRET}', '--build-arg', 'NEXT_PUBLIC_HOST=${_NEXT_PUBLIC_HOST}', '.']
images:
- gcr.io/$PROJECT_ID/${_SERVICE_NAME}